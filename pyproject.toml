[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lucode"
version = "0.1.0"
description = "Lucode terminal coding agent"
requires-python = ">=3.11"
dependencies = [
  "openai-agents==0.14.6",
  "mcp>=1.27,<2",
  "prompt_toolkit>=3.0.52,<4",
  "requests>=2.33,<3",
  "rich>=15,<16",
]
license = { text = "UNLICENSED" }

[project.optional-dependencies]
gui = ["PySide6>=6.6", "qasync>=0.27"]
server = ["starlette>=0.37,<1", "uvicorn>=0.35,<1"]

[project.scripts]
lucode = "lucode.entry:main"
lucode-runtime-server = "runtime.server.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = [
  "catalogs*",
  "catalog_system*",
  "lucode*",
  "mcp_servers*",
  "planning*",
  "runtime*",
  "skills*",
]
exclude = [
  "tests*",
  "bin*",
  "scripts*",
  "lucode.frozen_entry",
  "skills.jpc_now_skill*",
  "skills.Humanizer-zh-main*",
  "skills.task-router*",
]

[tool.setuptools.package-data]
catalogs = ["*.json", "*.md"]
skills = [
  "cli-command-safety/**/*",
  "code-engineer/**/*",
  "final-synthesizer/**/*",
  "full-supervisor/**/*",
  "full-worker-contract/**/*",
  "orchestrator-planner/**/*",
  "project-explorer/**/*",
  "query-refiner/**/*",
  "serial-executor-contract/**/*",
  "solo-executor-contract/**/*",
  "skill-creator/**/*",
]

[tool.setuptools.exclude-package-data]
"*" = ["__pycache__/*", "*.pyc", "*.pyo"]
